{
  "name": "proto005-image-blocks",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "compile-levels": "node --import tsx ./scripts/compile-levels.ts",
    "dev": "npm run compile-levels && vite",
    "build": "npm run compile-levels && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "phaser": "^4.1.0"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3",
    "vite": "^8.0.12"
  }
}
